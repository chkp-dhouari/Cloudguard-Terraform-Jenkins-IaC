pipeline {
 agent any
 
 stages {
   stage(‘checkout  dev branch’) {
     steps {
       checkout scm
 
      }
 
    }
 stage(‘ver’) {
   steps {
 
     sh ‘terraform — version’
 
 
       }
     }
 
 
 
 
 
  }
}
